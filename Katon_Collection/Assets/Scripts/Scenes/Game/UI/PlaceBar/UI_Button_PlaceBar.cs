using System.Collections;
using System.Collections.Generic;
using UnityEngine;


//UI_Buttonを継承しているクラス

public class UI_Button_PlaceBar : UI_Button
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
